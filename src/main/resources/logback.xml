<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender with pattern -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Application loggers -->
    <logger name="com.blitzlines" level="INFO" />
    
    <!-- Kafka Streams loggers - reduce noise -->
    <logger name="org.apache.kafka.streams" level="WARN" />
    <logger name="org.apache.kafka.clients" level="WARN" />
    <logger name="org.apache.kafka.common" level="WARN" />
    
    <!-- RocksDB logger -->
    <logger name="org.rocksdb" level="WARN" />

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
